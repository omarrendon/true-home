// import second from '@testing-library/react-';
;

describe('Test en el hook usCounuter', () => {
  test('Debe de retornar los valores por defecto', () => {
    // const { counter, increaseBy } 
  });
  
});